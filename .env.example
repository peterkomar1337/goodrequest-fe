# Base URL of the GoodRequest assignment API. The app falls back to this same
# value when the variable is missing, so cloning and running works without it.
NEXT_PUBLIC_API_URL=https://frontend-assignment-api.goodrequest.dev/api/v1
