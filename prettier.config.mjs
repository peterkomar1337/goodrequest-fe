/**
 * Prettier runs on defaults on purpose: 2 spaces, double quotes, semicolons,
 * 80 column width. Every rule added here is a formatting discussion reopened,
 * which is the one thing Prettier exists to prevent.
 *
 * Written as .mjs rather than .prettierrc so this note can live next to it.
 *
 * @type {import("prettier").Config}
 */
const config = {};

export default config;
